int y=215;
int x=215;
void setup (){
 size(800,600);
}
void draw(){
   background(255);
   rect(45,y,30,60);
    fill(0,0,255);
  if (keyPressed && (key == CODED)) {
    if (keyCode ==UP){
      y--;
     
    }else if (keyCode == DOWN){
    y++;
    
    }
}
